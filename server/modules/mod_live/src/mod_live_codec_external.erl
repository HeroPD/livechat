%% Created automatically by XML generator (fxml_gen.erl)
%% Source: mod_live_codec.spec

-module(mod_live_codec_external).

-compile(export_all).

modules() -> [].

lookup(_, _) -> undefined.

lookup(_) -> undefined.
